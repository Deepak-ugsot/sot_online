/**
 * Public API for the leaders feature.
 *
 * Routes import from `@/features/leaders` only — never from an internal component path.
 */
export { LeadersSection } from "./components/leaders-section";
export type { Leader, LeadersHeadingCopy } from "./types/leaders.types";
