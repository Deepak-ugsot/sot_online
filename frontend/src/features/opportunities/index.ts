/**
 * Public API for the opportunities feature.
 *
 * Routes import from `@/features/opportunities` only — never from an internal
 * component path.
 */
export { OpportunitiesSection } from "./components/opportunities-section";
export type { Opportunity } from "./types/opportunities.types";
