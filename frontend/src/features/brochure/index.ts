/**
 * Public API for the brochure feature.
 *
 * Routes import from `@/features/brochure` only — never from an internal component
 * path. Same contract as `@/features/auth`.
 */
export { BrochureScreen } from "./components/brochure-screen";
