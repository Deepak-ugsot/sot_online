/**
 * Public API for the AI Mentor feature.
 *
 * Routes import from `@/features/ai-mentor` only — never from an internal component path.
 */
export { AiMentorSection } from "./components/ai-mentor-section";
