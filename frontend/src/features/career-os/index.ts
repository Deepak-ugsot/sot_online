/**
 * Public API for the Career OS feature.
 *
 * Routes import from `@/features/career-os` only — never from an internal component path.
 */
export { CareerOsSection } from "./components/career-os-section";
export type {
  CareerOsColumn,
  CareerOsFeature,
  CareerOsIllustrationName,
  CareerOsMediaHeight,
} from "./types/career-os.types";
