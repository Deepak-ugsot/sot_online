/**
 * Public API for the compete feature.
 *
 * Routes import from `@/features/compete` only — never from an internal component
 * path.
 */
export { CompeteSection } from "./components/compete-section";
export type { CompeteColumn, CompeteHighlight } from "./types/compete.types";
