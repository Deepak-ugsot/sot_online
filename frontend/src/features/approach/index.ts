/**
 * Public API for the approach feature.
 *
 * Routes import from `@/features/approach` only — never from an internal component path.
 */
export { ApproachSection } from "./components/approach-section";
export type { ApproachWord } from "./types/approach.types";
