/**
 * Public API for the dashboard feature.
 *
 * Routes import from `@/features/dashboard` only — never from an internal component path.
 */
export { DashboardSection } from "./components/dashboard-section";
export type { DashboardChecklistRow } from "./types/dashboard.types";
