/**
 * Public API for the gallery feature.
 *
 * Routes import from `@/features/gallery` only — never from an internal component path.
 */
export { GallerySection } from "./components/gallery-section";
export type { GalleryCard, GalleryHeadingCopy } from "./types/gallery.types";
