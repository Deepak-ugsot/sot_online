/**
 * Public API for the showcase feature.
 *
 * Routes import from `@/features/showcase` only — never from an internal component path.
 */
export { ShowcaseSection } from "./components/showcase-section";
export type {
  ShowcaseHeadingCopy,
  ShowcaseProject,
} from "./types/showcase.types";
