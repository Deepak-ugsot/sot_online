/**
 * Public API for the hero feature.
 *
 * Routes import from `@/features/hero` only — never from an internal component path.
 */
export { HeroSection } from "./components/hero-section";
export type { HeroCta, HeroHeadlineCopy } from "./types/hero.types";
