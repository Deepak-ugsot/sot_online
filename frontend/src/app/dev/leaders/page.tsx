import { LeadersSection } from "@/features/leaders";

/**
 * Temporary isolation harness for the Leaders section. Not linked from anywhere.
 * Mirrors `dev/career-os`.
 */
export default function LeadersDevPage() {
  return (
    <main>
      <LeadersSection />
    </main>
  );
}
