import { EcosystemSection } from "@/features/ecosystem";

/**
 * Temporary isolation harness for the Ecosystem section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function EcosystemDevPage() {
  return (
    <main>
      <EcosystemSection />
    </main>
  );
}
