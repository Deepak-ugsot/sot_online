import { CompeteSection } from "@/features/compete";

/**
 * Temporary isolation harness for the Compete section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function CompeteDevPage() {
  return (
    <main>
      <CompeteSection />
    </main>
  );
}
