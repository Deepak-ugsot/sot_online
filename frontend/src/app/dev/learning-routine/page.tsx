import { LearningRoutineSection } from "@/features/learning-routine";

/**
 * Temporary isolation harness for the Learning Routine section. Not linked from
 * anywhere. Mirrors `dev/hackathons`.
 */
export default function LearningRoutineDevPage() {
  return (
    <main>
      <LearningRoutineSection />
    </main>
  );
}
