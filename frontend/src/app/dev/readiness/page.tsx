import { ReadinessSection } from "@/features/readiness";

/**
 * Temporary isolation harness for the Readiness section. Not linked from anywhere.
 * Mirrors `dev/gallery`.
 */
export default function ReadinessDevPage() {
  return (
    <main>
      <ReadinessSection />
    </main>
  );
}
