import { OneProgramSection } from "@/features/one-program";

/**
 * Temporary isolation harness for the One Program section. Not linked from anywhere.
 * Mirrors `dev/learn-from-people`.
 */
export default function OneProgramDevPage() {
  return (
    <main>
      <OneProgramSection />
    </main>
  );
}
