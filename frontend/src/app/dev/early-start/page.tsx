import { EarlyStartSection } from "@/features/early-start";

/**
 * Temporary isolation harness for the Early Start section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function EarlyStartDevPage() {
  return (
    <main>
      <EarlyStartSection />
    </main>
  );
}
