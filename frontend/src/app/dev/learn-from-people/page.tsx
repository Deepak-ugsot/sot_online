import { LearnFromPeopleSection } from "@/features/learn-from-people";

/**
 * Temporary isolation harness for the Learn From People section. Not linked from
 * anywhere. Mirrors `dev/curriculum`.
 */
export default function LearnFromPeopleDevPage() {
  return (
    <main>
      <LearnFromPeopleSection />
    </main>
  );
}
