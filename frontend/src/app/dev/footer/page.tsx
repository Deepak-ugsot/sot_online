import { SiteFooter } from "@/features/footer";

/**
 * Temporary isolation harness for the site footer. Not linked from anywhere.
 * Mirrors `dev/compete`.
 */
export default function FooterDevPage() {
  return <SiteFooter />;
}
