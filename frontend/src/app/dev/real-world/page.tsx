import { RealWorldSection } from "@/features/real-world";

/**
 * Temporary isolation harness for the Real World section. Not linked from anywhere.
 * Mirrors `dev/early-start`.
 */
export default function RealWorldDevPage() {
  return (
    <main>
      <RealWorldSection />
    </main>
  );
}
