import { PortfolioSection } from "@/features/portfolio";

/**
 * Temporary isolation harness for the Portfolio section. Not linked from anywhere.
 * Mirrors `dev/learning-routine`.
 */
export default function PortfolioDevPage() {
  return (
    <main>
      <PortfolioSection />
    </main>
  );
}
