import { TransformationSection } from "@/features/transformation";

/**
 * Temporary isolation harness for the Transformation section. Not linked from
 * anywhere. Mirrors `dev/curriculum`.
 */
export default function TransformationDevPage() {
  return (
    <main>
      <TransformationSection />
    </main>
  );
}
