import { AiMentorSection } from "@/features/ai-mentor";

/**
 * Temporary isolation harness for the AI Mentor section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function AiMentorDevPage() {
  return (
    <main>
      <AiMentorSection />
    </main>
  );
}
