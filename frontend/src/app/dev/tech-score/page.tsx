import { TechScoreSection } from "@/features/tech-score";

/**
 * Temporary isolation harness for the Tech Score section. Not linked from anywhere.
 * Mirrors `dev/career-os`.
 */
export default function TechScoreDevPage() {
  return (
    <main>
      <TechScoreSection />
    </main>
  );
}
