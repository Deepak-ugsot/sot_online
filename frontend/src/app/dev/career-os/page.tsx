import { CareerOsSection } from "@/features/career-os";

/**
 * Temporary isolation harness for the Career OS section. Not linked from anywhere.
 * Mirrors `dev/ecosystem`.
 */
export default function CareerOsDevPage() {
  return (
    <main>
      <CareerOsSection />
    </main>
  );
}
