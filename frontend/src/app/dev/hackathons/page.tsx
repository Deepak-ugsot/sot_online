import { HackathonsSection } from "@/features/hackathons";

/**
 * Temporary isolation harness for the Hackathons section. Not linked from anywhere.
 * Mirrors `dev/next-opportunity`.
 */
export default function HackathonsDevPage() {
  return (
    <main>
      <HackathonsSection />
    </main>
  );
}
