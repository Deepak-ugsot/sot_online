import { FacultySection } from "@/features/faculty";

/**
 * Temporary isolation harness for the Faculty section. Not linked from anywhere.
 * Mirrors `dev/leaders`.
 */
export default function FacultyDevPage() {
  return (
    <main>
      <FacultySection />
    </main>
  );
}
