import { GallerySection } from "@/features/gallery";

/**
 * Temporary isolation harness for the Gallery section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function GalleryDevPage() {
  return (
    <main>
      <GallerySection />
    </main>
  );
}
