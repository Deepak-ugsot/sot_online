import { ApproachSection } from "@/features/approach";

/**
 * Temporary isolation harness for the Approach section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function ApproachDevPage() {
  return (
    <main>
      <ApproachSection />
    </main>
  );
}
