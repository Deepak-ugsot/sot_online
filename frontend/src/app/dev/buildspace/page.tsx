import { BuildspaceSection } from "@/features/buildspace";

/**
 * Temporary isolation harness for the BuildSpace section. Not linked from anywhere.
 * Mirrors `dev/curriculum`.
 */
export default function BuildspaceDevPage() {
  return (
    <main>
      <BuildspaceSection />
    </main>
  );
}
